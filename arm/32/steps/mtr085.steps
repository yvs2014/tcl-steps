:

set -e

NAME="mtr085"
VER="0.85.154"
GH_USER="yvs2014"

. ./func1.inc

RUN_DEPS="ncurses libidn2"
BUILD_DEPS="autoconf automake libcap-dev ncurses-dev libidn2-dev"

std_build
top_n_clean
prep_gh_git

autoreconf -fi
./configure --disable-ipv6 --with-libidn
make
sudo make DESTDIR="/tmp/$DST" install
cd ..
mksquashfs "$DST" "$TCZ" -xattrs

mk_tcz
sudo rm -rf "$DST"

