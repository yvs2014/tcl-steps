:

set -e

NAME="pingpath"
VER="0.3.20"
GH_USER="yvs2014"
NOT_FOR="i686"

. "$(dirname $0)/steps.common"

RUN_DEPS="iputils-ping cglm gtk4 gsettings-desktop-schemas json-glib"
BUILD_DEPS="autoconf automake pkg-config gtk4-dev json-glib-dev"
tce-load -i cglm # it's not present in distributive repo

std_build
top_n_clean
prep_gh_git

sed -i 's,"/bin/ping","/usr/local/bin/ping",' pinger.c
sed -i '/ln -srf/d' Makefile
sed -i 's/644 -T/644/' Makefile
make
make "DESTDIR=/tmp/$DST" install

mk_tcz

